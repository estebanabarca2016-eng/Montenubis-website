# Montenubis v12.1

Montenubis boutique reserve website — Vite + React.

Latest cleanup:
- Tours hero uses the Montenubis sunset image instead of the removed trail image.
- The removed trail image is deleted from the project assets and source references.
- The mobile header stays compact and prevents ENG/ESP, Reserve and the menu from overlapping MonteNubis.
- Our Story includes the supplied Liam Abarca family photo in the Next Generation section.

## Run locally

```bash
npm install
npm run dev
```
